# Read the documentation at https://github.com/thephpleague/flysystem-bundle/blob/master/docs/1-getting-started.md
flysystem:
    storages:
        default.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/var/storage/default'
                permissions:
                    file:
                        public: 0o777
                        private: 0o777
                    dir:
                        public: 0o777
                        private: 0o777
        picture.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/../picture'
                permissions:
                    file:
                        public: 0o644
                        private: 0o644
                    dir:
                        public: 0o755
                        private: 0o755
        proof.s3.storage:
            adapter: 'aws'
            options:
                client: 'Aws\S3\S3Client'
                bucket: '%env(AWS_S3_BUCKET_PROOF)%'
                prefix: ''
                streamReads: true
        proof.storage:
            adapter: 'lazy'
            options:
                source: '%env(PROOF_STORAGE)%'
