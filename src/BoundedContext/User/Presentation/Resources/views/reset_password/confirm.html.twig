{% extends '@SharedKernel/base.html.twig' %}
{% trans_default_domain 'User' %}

{% block title %}
    {{ 'reset_password.confirm.title'|trans }} - Video Games Records
{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h1 class="card-title text-center mb-2">{{ 'reset_password.confirm.title'|trans }}</h1>
                    <p class="text-center text-muted mb-4">{{ 'reset_password.confirm.subtitle'|trans }}</p>

                    {{ form_start(form, {'attr': {'data-turbo': 'false'}}) }}

                        {# Display global form errors #}
                        {{ form_errors(form) }}

                        <div class="mb-3">
                            {{ form_label(form.plainPassword.first, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.plainPassword.first) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.plainPassword.second, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.plainPassword.second) }}
                            <div class="form-text">{{ 'reset_password.confirm.help'|trans }}</div>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                {{ 'reset_password.confirm.submit'|trans }}
                            </button>
                        </div>

                    {{ form_end(form) }}

                    <div class="text-center mt-3">
                        <p class="mb-0">
                            <a href="{{ path('app_login') }}" class="text-decoration-none fw-bold">
                                {{ 'reset_password.confirm.back_to_login'|trans }}
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
