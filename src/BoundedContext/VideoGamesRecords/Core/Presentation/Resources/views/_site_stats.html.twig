<div class="d-none d-md-block mb-4">
    <div class="row">
        <div class="col-md-8 col-lg-6">
            <p class="text-muted mb-2">
                {{ 'site_stats.description'|trans({}, 'VgrCore') }}
            </p>
            <p class="text-muted mb-0">
                {{ 'site_stats.summary'|trans({
                    '%nbPlayers%': nbPlayers|number_format(0, '.', ','),
                    '%nbScores%': nbScores|number_format(0, '.', ','),
                    '%nbGames%': nbGames|number_format(0, '.', ','),
                }, 'VgrCore')|raw }}
            </p>
        </div>
    </div>
</div>