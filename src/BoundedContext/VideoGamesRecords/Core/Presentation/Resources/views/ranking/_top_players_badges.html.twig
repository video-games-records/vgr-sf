<div class="card border shadow-sm h-100">
    <div class="card-header border-bottom">
        <h5 class="mb-0"><i class="bi bi-patch-check"></i> {{ 'ranking.top_players.badges.title'|trans({}, 'VgrCore') }}</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th scope="col" class="text-center" style="width: 50px;">#</th>
                        <th scope="col">{{ 'ranking.top_players.player'|trans({}, 'VgrCore') }}</th>
                        <th scope="col" class="text-end" style="width: 70px;" title="{{ 'ranking.top_players.points'|trans({}, 'VgrCore') }}">{{ 'ranking.top_players.pts'|trans({}, 'VgrCore') }}</th>
                        <th scope="col" class="text-center" style="width: 60px;" title="{{ 'ranking.top_players.master_badges'|trans({}, 'VgrCore') }}"><i class="bi bi-star-fill"></i></th>
                    </tr>
                </thead>
                <tbody>
                    {% for player in players %}
                        <tr>
                            <td class="text-center">
                                <span class="badge rounded-pill {% if loop.index == 1 %}bg-warning text-dark{% else %}bg-secondary{% endif %}">
                                    {{ player.rankBadge }}
                                </span>
                            </td>
                            <td>
                                {{ component('player_link', {
                                    player: player,
                                    showTeam: false,
                                    showCountry: false
                                }) }}
                            </td>
                            <td class="text-end">{{ player.pointBadge|number_format(0, '.', ',') }}</td>
                            <td class="text-end"><span class="badge bg-primary">{{ player.nbMasterBadge }}</span></td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted py-4">
                                {{ 'ranking.top_players.no_players'|trans({}, 'VgrCore') }}
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
